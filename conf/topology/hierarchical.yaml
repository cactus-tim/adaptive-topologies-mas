name: hierarchical
max_iterations: 20
extra:
  sub_teams:
    - team_id: team_a
      workers:
        - executor_a1
        - executor_a2
    - team_id: team_b
      workers:
        - executor_b1
        - executor_b2
  finalize_signal: top_coord_finalize
  final_answer_strategy: json_concat
  max_rounds: 4
