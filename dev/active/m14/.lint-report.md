# M14 Lint Report — 2026-05-17

## Result: CLEAN

All ruff and mypy checks pass after auto-fixes.

## Summary

- **ruff check --fix**: 67 issues auto-fixed across m14 files; 25 remaining manually fixed
- **ruff format**: 19 files reformatted
- **mypy**: 1 issue fixed (unused `type: ignore` comment in runner.py)
- **Unit tests**: 291 passed, 0 failed

## Auto-fixed by ruff (67 issues)

Ruff auto-fixed import ordering (I), unused imports (F401), and other
mechanical issues across the full m14 file set.

## Manually fixed (25 issues)

### src/atm/ui/app.py
- E402: Added `# noqa: E402` to post-`nest_asyncio.apply()` imports; moved
  `contextlib` import before the `nest_asyncio` call
- SIM105: Replaced `try/except/pass` with `contextlib.suppress(RuntimeError)`
- SIM102: Collapsed nested `if` statements into a single combined condition

### src/atm/ui/tlx.py
- RUF002: Replaced EN dash (`–`) with hyphen-minus (`-`) in docstring

### src/atm/ui/views.py
- F841: Removed unused `participant_id` variable in `render_response()`
- SIM102 (×2): Collapsed two sets of nested `if`/`elif`+`if` patterns in the
  proctor panel button handlers

### tests/unit/human/test_queue.py
- F841: Removed unused `call_count = 0` variable
- SIM117: Merged nested `with pytest.raises(...)` / `with patch(...)` into a
  single `with` statement with two contexts

### tests/unit/topology/test_topology_fallback_no_llm.py
- F841 (×2): Removed unused `result = asyncio.run(node_fn(state))` assignments
  (result was not asserted on)
- N801 (×8): Renamed test classes to CapWords (removed underscores between
  site letter and descriptor):
  - `TestFallbackSiteA_NodeFactory` → `TestFallbackSiteANodeFactory`
  - `TestFallbackSiteB_Chain` → `TestFallbackSiteBChain`
  - `TestFallbackSiteC_Mesh` → `TestFallbackSiteCMesh`
  - `TestFallbackSiteD_DebateHumanJudge` → `TestFallbackSiteDDebateHumanJudge`
  - `TestFallbackSiteE_DebateBothMode` → `TestFallbackSiteEDebateBothMode`
  - `TestFallbackSiteF_HierarchicalTop` → `TestFallbackSiteFHierarchicalTop`
  - `TestFallbackSiteG_HierarchicalSub` → `TestFallbackSiteGHierarchicalSub`
  - `TestFallbackSiteH_Adaptive` → `TestFallbackSiteHAdaptive`

### tests/unit/topology/test_topology_streamlit_dispatch.py
- B905: Added `strict=True` to `zip()` call

### tests/unit/ui/test_tlx_form.py
- C408 (×3): Replaced `dict(k=v, ...)` constructor calls with dict literals `{...}`

### src/atm/experiment/runner.py
- mypy unused-ignore: Removed `# type: ignore[union-attr]` from line 186
  (mypy determined `human_cfg` is not None in that context)

## Unfixed issues

None.
