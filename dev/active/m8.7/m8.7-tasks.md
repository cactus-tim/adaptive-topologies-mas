# M8.7 Oracle Labels Pipeline — Tasks

## Phase 1: Research and TDD Red (parallel wave) NOT STARTED

- [x] 1.1 Verify `Phase` string form and pin `_VALID_TOPOLOGIES` constants — confirmed: `Phase.PLANNING == "planning"`, `_VALID_TOPOLOGIES = {linear, supervisor, mesh, debate, hierarchical}`

- [x] 1.2 Verify external library APIs — confirmed: pydantic 2.13.3, sqlalchemy 2.0.49, pyyaml 6.0.3

- [ ] 1.3 Write failing unit tests (TDD red phase) — `tests/unit/analysis/__init__.py` (new), `tests/unit/analysis/test_oracle.py` (new, 9 tests)
  - Acceptance: `uv run pytest tests/unit/analysis/test_oracle.py -v` shows all 9 tests FAIL with ImportError

- [ ] 1.4 Create `conf/oracle/type_level_manual.yaml` skeleton — `conf/oracle/type_level_manual.yaml` (new dir + file)
  - Acceptance: `python -c "import yaml; yaml.safe_load(open('conf/oracle/type_level_manual.yaml'))"` succeeds; 12 TODO cells present; file under ~40 lines


## Phase 2: Implementation (TDD green + refactor) NOT STARTED

- [x] 2.1 Implement `src/atm/analysis/oracle.py` — `src/atm/analysis/oracle.py` (new)
  - Acceptance: `uv run pytest tests/unit/analysis/test_oracle.py -v` all 9 pass; `uv run ruff check src/atm/analysis/oracle.py` clean; `uv run mypy src/atm/analysis/oracle.py` clean

- [ ] 2.2 Wire `src/atm/analysis/__init__.py` exports and update `dev/codebase-map.md` — `src/atm/analysis/__init__.py` (edit), `dev/codebase-map.md` (edit)
  - Acceptance: `uv run python -c "from atm.analysis import OracleTable, load_oracle_table, build_leave_one_out_oracle"` exits 0; codebase-map has analysis layer entry under Key Modules


## Phase 3: Integration Test NOT STARTED

- [ ] 3.1 Write Postgres-backed integration test — `tests/integration/analysis/__init__.py` (new), `tests/integration/analysis/test_oracle_integration.py` (new)
  - Acceptance: `ATM_INTEGRATION_PG=1 uv run pytest tests/integration/analysis/ -v` passes; `uv run pytest tests/integration/analysis/ -v` shows SKIPPED (via `session_factory_fast` -> `pg_engine_fast` -> `pg_dsn` chain)

---

## Stats

- Total: 7 tasks · ~4h
- Done: 0 / 7

## How to Update

Check off tasks with [x] and update `m8.7-context.md` SESSION PROGRESS after each milestone.

Phase header legend:
- `NOT STARTED` — no tasks started
- `IN PROGRESS` — some tasks done or in progress
- `COMPLETE` — all tasks in phase done with [x]
