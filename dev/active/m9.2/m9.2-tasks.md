# m9.2 — Adaptive Role Router — Задачи

## Wave 1: Фундамент — Protocol, HumanCfg, миграция БД — NOT STARTED

- [x] 1.1 Создать `HumanRoleRouter` Protocol + `FixedRoleRouter` — `src/atm/human/role_router.py`
  - Тип: tdd
  - Зависит от: —
  - Параллельно с: 1.2, 1.3
  - Acceptance: `uv run pytest tests/unit/human/test_role_router.py -k fixed -v` зелёный; mypy без ошибок
  - Файлы: `src/atm/human/role_router.py` (CREATE), `src/atm/human/__init__.py` (EDIT), `tests/unit/human/test_role_router.py` (CREATE)
  - Заметки: Скопировать структуру `PhaseRouter` (Protocol + `@runtime_checkable`); `FixedRoleRouter.decide()` игнорирует аргументы

- [ ] 1.2 Расширить `HumanCfg` тремя полями — `src/atm/experiment/config.py`
  - Тип: simple
  - Зависит от: —
  - Параллельно с: 1.1, 1.3
  - Acceptance: дефолт `role_router=="fixed"`, `role_table is None`, `role_router_model is None`; существующие тесты зелёные
  - Файлы: `src/atm/experiment/config.py` (EDIT), `tests/unit/experiment/test_config_human.py` (EDIT)
  - Заметки: `role_table: dict[str, str] | None` (строки, не enum'ы — OmegaConf совместимость); конверт в `_build_role_router` (шаг 4.1)

- [ ] 1.3 Alembic миграция `0003` + `Run.cognitive_load_proxy` — `src/atm/storage/models.py`
  - Тип: simple
  - Зависит от: —
  - Параллельно с: 1.1, 1.2
  - Acceptance: `alembic upgrade head` → `downgrade -1` → `upgrade head` без ошибок; `Run.cognitive_load_proxy: Mapped[float | None]`
  - Файлы: `alembic/versions/0003_runs_cognitive_load_proxy.py` (CREATE), `src/atm/storage/models.py` (EDIT)
  - Заметки: `revision="0003"`, `down_revision="0002"`; использовать `sa.Float()` (не Double) для SQLite-совместимости (фикс M6)

---

## Wave 2: Rule router + метрика cognitive_load — NOT STARTED

- [x] 2.1 `RuleBasedRoleRouter` + `conf/human/role_table.yaml` — `src/atm/human/role_router.py`
  - Тип: tdd
  - Зависит от: 1.1
  - Параллельно с: 2.2
  - Acceptance: `uv run pytest tests/unit/human/test_role_router.py -k rule -v`; YAML с неизвестной фазой → ValueError
  - Файлы: `src/atm/human/role_router.py` (EDIT), `conf/human/role_table.yaml` (CREATE), `tests/unit/human/test_role_router.py` (EDIT)
  - Заметки: `DEFAULT_ROLE_TABLE = {PLANNING: COORDINATOR, EXECUTION: PEER, VERIFICATION: REVIEWER, DONE: REVIEWER}`; `decide()` использует `self._table.get(phase, self._fallback)`

- [x] 2.2 `human_sim_cognitive_load_proxy` + `conf/evaluation/cognitive_load.yaml` — `src/atm/evaluation/metrics.py`
  - Тип: tdd
  - Зависит от: 1.3
  - Параллельно с: 2.1
  - Acceptance: тест k=3 фикстура на in-memory SQLite даёт ожидаемое число; `uv run pytest tests/unit/evaluation/test_cognitive_load_proxy.py -v`
  - Файлы: `src/atm/evaluation/metrics.py` (CREATE), `src/atm/evaluation/__init__.py` (EDIT), `conf/evaluation/cognitive_load.yaml` (CREATE), `conf/evaluation/` директория (CREATE), `tests/unit/evaluation/test_cognitive_load_proxy.py` (CREATE)
  - Заметки: формула `α*count + β*mean(len(json.dumps(ctx))) + γ*mean(latency_s)`; latency=0.0 если `answered_at is None`; `if not rows: return 0.0`

---

## Wave 3: LLM router с fallback — NOT STARTED

- [x] 3.1 `LLMRoleRouter` с Pydantic-валидацией и fallback на rule — `src/atm/human/role_router.py`
  - Тип: tdd
  - Зависит от: 2.1
  - Параллельно с: —
  - Acceptance: 4 fallback-пути покрыты тестами; `uv run pytest tests/unit/human/test_role_router.py -v` полностью зелёный
  - Файлы: `src/atm/human/role_router.py` (EDIT), `tests/unit/human/test_role_router.py` (EDIT), `tests/fixtures/llm/m9_2_role_router_valid.yaml` (CREATE), `tests/fixtures/llm/m9_2_role_router_invalid.yaml` (CREATE)
  - Заметки: скопировать `LLMPhaseRouter`; `agent_id="role_router"`; `_RoleRouterDecision(BaseModel)` с `role: HumanRole`, `reason: str = ""`; любая ошибка → WARNING + `return await self._fallback.decide(phase, state)`

---

## Wave 4: Runner factory — NOT STARTED

- [ ] 4.1 `_build_role_router(cfg, ...) -> HumanRoleRouter | None` + проброс в topology.build — `src/atm/experiment/runner.py`
  - Тип: tdd
  - Зависит от: 1.1, 1.2, 2.1, 3.1
  - Параллельно с: —
  - Acceptance: 4 теста (fixed→None, rule→RuleBased, llm→LLM, unknown→ValueError); existing runner tests зелёные
  - Файлы: `src/atm/experiment/runner.py` (EDIT), `tests/unit/experiment/test_runner_role_router.py` (CREATE)
  - Заметки: `fixed` → `None` (back-compat short-circuit); для `rule` — `RuleBasedRoleRouter(table=..., fallback=cfg.human.role)`; для `llm` — мок `build_llm` в тестах; передать `role_router` в `topology_instance.build(..., role_router=router)`

---

## Wave 5: Интеграция router в топологии (параллельно: 5.1, 5.2, 5.3, 5.4) — NOT STARTED

- [ ] 5.1 Chain topology + `_node_factory` + Star — паттерн `if role_router is not None`
  - Тип: tdd
  - Зависит от: 1.1, 1.2, 2.1, 3.1, 4.1
  - Параллельно с: 5.2, 5.3, 5.4
  - Acceptance: back-compat (router=None) → роль из cfg; dynamic → роль из router.decide(); тесты chain/star/node_factory зелёные
  - Файлы: `src/atm/topology/chain.py` (EDIT), `src/atm/human/_node_factory.py` (EDIT), `src/atm/topology/star.py` (EDIT), `tests/unit/topology/test_chain_human.py` (EDIT), `tests/unit/topology/test_star_human.py` (EDIT), `tests/unit/human/test_node_factory.py` (EDIT, фикс M4)
  - Заметки: `Phase(shared.get("phase","execution"))` для приведения строки; роль попадает в `human_interactions.role` через стандартный callback; `SharedState` НЕ изменяется

- [ ] 5.2 Mesh topology — human_peer closure
  - Тип: tdd
  - Зависит от: 1.1, 1.2, 2.1, 3.1, 4.1
  - Параллельно с: 5.1, 5.3, 5.4
  - Acceptance: back-compat + dynamic тесты; `uv run pytest tests/unit/topology/test_mesh_human.py -v`
  - Файлы: `src/atm/topology/mesh.py` (EDIT), `tests/unit/topology/test_mesh_human.py` (EDIT)
  - Заметки: line ~386; `Phase(shared.get("phase", "execution"))`

- [ ] 5.3 Debate + Hierarchical — outer closure capture (фиксы C3, C4)
  - Тип: tdd
  - Зависит от: 1.1, 1.2, 2.1, 3.1, 4.1
  - Параллельно с: 5.1, 5.2, 5.4
  - Acceptance: 4 теста (back-compat + dynamic × 2 топологии); тесты debate/hierarchical зелёные
  - Файлы: `src/atm/topology/debate.py` (EDIT — обе HITL-точки; outer scope capture в build()), `src/atm/topology/hierarchical.py` (EDIT — `_build_subgraph_with_human` + `_build_human_sub_reviewer_node` получают `role_router`), `tests/unit/topology/test_debate_human.py` (EDIT), `tests/unit/topology/test_hierarchical_human.py` (EDIT)
  - Заметки: Debate line 387 + line 870 (`_both_judge_postprocess`) — `role_router` в outer build() scope, не новый аргумент; Hierarchical — `_build_subgraph_with_human(role_router=role_router)` → `_build_human_sub_reviewer_node(role_router=role_router)`

- [ ] 5.4 Adaptive topology — human_advisor closure
  - Тип: tdd
  - Зависит от: 1.1, 1.2, 2.1, 3.1, 4.1
  - Параллельно с: 5.1, 5.2, 5.3
  - Acceptance: back-compat + dynamic тест; `uv run pytest tests/unit/topology/test_adaptive_human.py -v`
  - Файлы: `src/atm/topology/adaptive.py` (EDIT — line ~569), `tests/unit/topology/test_adaptive_human.py` (EDIT)
  - Заметки: Adaptive переключает topology — router в outer build() scope; phase из shared.get("phase", "planning")

---

## Wave 6: Runner финализация — NOT STARTED

- [ ] 6.1 Финальный UPDATE `runs.human_role` (SQL к `human_interactions`) + `cognitive_load_proxy`
  - Тип: tdd
  - Зависит от: 1.3, 2.2, 4.1, 5.1, 5.2, 5.3, 5.4
  - Параллельно с: —
  - Acceptance: `_update_run_success` принимает `human_role: str | None`, `cognitive_load_proxy: float | None`; оба блока в try/except (фикс M2); budget-failed → NULL (фикс M3); `uv run pytest tests/unit/experiment/test_runner_human_role.py -v`
  - Файлы: `src/atm/experiment/runner.py` (EDIT), `tests/unit/experiment/test_runner_human_role.py` (EDIT/CREATE)
  - Заметки: `SELECT role FROM human_interactions WHERE run_id=:rid ORDER BY requested_at DESC LIMIT 1`; если пусто → `dynamic_role = None` (роль остаётся из `_insert_run`); load weights с fallback на дефолт если файл отсутствует; `_update_run_failed` не изменяется

---

## Wave 7: Интеграционный тест + регрессия (параллельно: 7.1, 7.2) — NOT STARTED

- [ ] 7.1 Integration test — Star + Rule router + cognitive_load_proxy (`requires_postgres`)
  - Тип: tdd
  - Зависит от: 4.1, 5.1, 6.1
  - Параллельно с: 7.2
  - Acceptance: `len(set(role_values)) >= 2`; `run_obj.human_role == role_values[-1]`; `cognitive_load_proxy > 0`; `ATM_ENABLE_PG_TESTS=1 uv run pytest tests/integration/human/test_role_router_star_e2e.py -v -m requires_postgres` зелёный
  - Файлы: `tests/integration/human/test_role_router_star_e2e.py` (CREATE, фикс M7 — Star не Chain), `tests/fixtures/llm/m9_2_star_role_router.yaml` (CREATE, включает `agent_id="role_router"` entries, фикс M8)
  - Заметки: опираться на `test_hitl_cross_topology.py` как образец; Star имеет фазовые переходы → ≥2 роли; если срабатывает бюджет/iter cap — увеличить лимиты в фикстуре

- [ ] 7.2 Back-compat regression test + полный smoke прогон
  - Тип: tdd
  - Зависит от: 5.1, 5.2, 5.3, 5.4, 6.1
  - Параллельно с: 7.1
  - Acceptance: default HumanCfg → `_build_role_router` returns `None`; `uv run pytest tests/unit/ -v` зелёный; ruff + mypy без ошибок
  - Файлы: `tests/unit/human/test_role_router_back_compat.py` (CREATE)
  - Заметки: исходники не изменяются; проверить что все m9.1 тесты (`test_hitl_cross_topology.py` + 5 per-topology) проходят без изменений

---

## Статистика

- Всего задач: 14
- Выполнено: 0 / 14
- Примерно: ~14h

## Как обновлять

После выполнения задачи:
1. Поменять `[ ]` на `[x]`
2. Если все задачи волны выполнены — заменить `NOT STARTED` на `COMPLETE`
3. Обновить `SESSION PROGRESS` в `m9.2-context.md`
4. Обновить статус файлов в разделе "Ключевые файлы" context.md
