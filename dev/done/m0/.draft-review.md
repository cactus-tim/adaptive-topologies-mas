# Review M0 — Bootstrap проекта (Revision 1, Iteration 2)

## Summary for Approval

**Executive summary**: Ревизия 1 плана M0 закрывает оба блокера предыдущего ревью. В dev-группе Step 3 явно добавлены `sqlalchemy[asyncio]>=2.0` и `asyncpg>=0.29` с обоснованием и расширенной Verification; в Step 2 Files/Components явно перечислены все шесть `.gitkeep`-файлов под `conf/`. Все четыре non-blocking рекомендации (комментарий про SQLAlchemy-specific DSN в `.env.example`, документация про исключение `alembic/` из mypy в Step 4, inline-комментарий про `filterwarnings=["error"]` в Step 8, зафиксированный порядок quickstart в Step 9) применены. Per-step `Can-Parallel-With` приведены в соответствие с wave-plan.

**Final Class**: `standard`.

**Needs Integration Tests**: `no` (scaffolding без published contracts).

**Numbered step list**:
1. (simple) Инициализация uv-проекта и базовый `pyproject.toml` → `pyproject.toml`, `.python-version`
2. (simple) Скелет каталогов `src/atm/`, `tests/`, `conf/`, `notebooks/` с placeholder-файлами → ~31 путь (15 `__init__.py` в `src/atm/`, 4 в `tests/`, 1 в `notebooks/.gitkeep`, 6 в `conf/*/.gitkeep`)
3. (simple) Dev/test dependency groups через `uv add --group ...` (включая `sqlalchemy[asyncio]>=2.0` и `asyncpg>=0.29`) → `pyproject.toml`, `uv.lock`
4. (simple) Конфиги ruff и mypy в `pyproject.toml` (с явным исключением `alembic/` через `files = ["src/atm"]`) → `pyproject.toml`
5. (simple) `docker-compose.yml` с Postgres 16 → `docker-compose.yml`
6. (simple) `alembic init -t async` + пустая initial migration → `alembic.ini`, `alembic/env.py`, `alembic/versions/*.py`
7. (simple) `.env.example` (с комментарием про SQLAlchemy-specific DSN) + `.gitignore` → `.env.example`, `.gitignore`
8. (**tdd**) pytest config + smoke-тест → `pyproject.toml`, `tests/unit/test_smoke.py`
9. (simple) `README.md` с quickstart на русском (зафиксированный порядок шагов) → `README.md`

**Parallelism graph summary (6 волн, max width 4)**:
- **Wave 1** (parallel, 4 worktrees): шаги 1, 2, 5, 7 — непересекающиеся файлы
- **Wave 2** (serial): шаг 3 — `pyproject.toml [dependency-groups]`
- **Wave 3** (serial): шаг 4 — `pyproject.toml [tool.ruff]/[tool.mypy]`
- **Wave 4** (serial): шаг 6 — `alembic/`
- **Wave 5** (serial): шаг 8 — `pyproject.toml [tool.pytest.ini_options]` + `tests/unit/test_smoke.py`
- **Wave 6** (serial): шаг 9 — `README.md`

**Files touched (укрупнённо)**:
- Корень: `pyproject.toml`, `uv.lock`, `.python-version`, `docker-compose.yml`, `.env.example`, `.gitignore`, `alembic.ini`, `README.md`
- `src/atm/` + 14 подпакетов: пустые `__init__.py`
- `tests/{unit,integration,fixtures}/`: `__init__.py` + `tests/unit/test_smoke.py`
- `alembic/{env.py, script.py.mako, README, versions/<ts>_initial.py}`
- `notebooks/.gitkeep`
- `conf/{,topology,agents,task,model,experiment}/.gitkeep`

**VERDICT**: `APPROVED`
**BLOCKING_ISSUES**: 0

---

## 1. Executive Summary

План M0 после ревизии 1 полностью готов к имплементации. Оба блокера закрыты с запасом по документации (rationale для включения sqlalchemy/asyncpg зафиксирован прямо в Implementation Notes Step 3, cross-reference между Step 3 Verification и Step 6 exit-criteria восстановлен). Все non-blocking рекомендации применены корректно, часто с дополнительным уточняющим комментарием. Language Policy и acceptance-таблица не потребовали правок.

## 2. Проверка закрытых блокеров

### BLOCKING #1 (Step 3: sqlalchemy + asyncpg в dev-group) — CLOSED

- `pyproject.toml` фрагмент в Implementation Notes Step 3: в группе `dev` присутствуют `"sqlalchemy[asyncio]>=2.0"` и `"asyncpg>=0.29"`.
- Команда установки обновлена: `uv add --group dev ruff mypy alembic 'sqlalchemy[asyncio]>=2.0' 'asyncpg>=0.29'`.
- Отдельный пункт Implementation Notes «Почему sqlalchemy+asyncpg уже на M0» — explicit rationale.
- Verification Step 3 расширена: `uv run python -c "import sqlalchemy.ext.asyncio, asyncpg; print('ok')"`.
- Step 6 Assumptions ссылается на Step 3 как на гаранта наличия пакетов.
- Testing Strategy пункт 6 теперь явно говорит «Требует `sqlalchemy[asyncio]>=2.0` + `asyncpg>=0.29` в dev-group (Step 3)».

### BLOCKING #2 (Step 2 Files/Components: conf/*/.gitkeep) — CLOSED

- В Files/Components Step 2 явно перечислены все шесть файлов: `conf/.gitkeep`, `conf/topology/.gitkeep`, `conf/agents/.gitkeep`, `conf/task/.gitkeep`, `conf/model/.gitkeep`, `conf/experiment/.gitkeep`.
- Implementation Notes: «`conf/` содержит **только** `.gitkeep` файлы в M0».
- Verification Step 2 расширена: `find conf -type d | sort`.
- Open Questions #5 сокращён до ссылки на Step 2.

## 3. Проверка non-blocking рекомендаций

1. **Комментарий в `.env.example` про SQLAlchemy-specific DSN формат** — APPLIED (Implementation Notes Step 7; Verification проверяет наличие через grep).
2. **Документация в Step 4: исключение alembic/ из mypy** — APPLIED (Implementation Notes Step 4 + inline-комментарий в TOML-блоке).
3. **Inline-комментарий рядом с `filterwarnings = ["error"]` в Step 8** — APPLIED (секция `[tool.pytest.ini_options]` + нарратив в Implementation Notes).
4. **Порядок quickstart в Step 9 README** — APPLIED: `cp .env.example .env` → `uv sync` → `docker compose up -d` → `uv run alembic upgrade head` (опц.) → `uv run pytest` → ruff/mypy.

## 4. Parallelism Audit (повторный)

Per-step `Can-Parallel-With` приведены в соответствие с wave-plan:

| Step | Declared Can-Parallel-With | Реальные конфликты | Оценка |
|---|---|---|---|
| 1 | 2, 5, 7 | Нет | OK |
| 2 | 1, 5, 7 | Нет | OK |
| 3 | none | `pyproject.toml` | OK |
| 4 | none | `pyproject.toml` | OK |
| 5 | 1, 2, 7 | Нет | OK |
| 6 | none | `pyproject.toml` + `alembic/` | OK |
| 7 | 1, 2, 5 | Нет | OK |
| 8 | none | `pyproject.toml` | OK |
| 9 | none | `README.md` | OK |

Wave 1 — 4 шага (< 7). Over-claim'ов больше нет.

## 5. Missing Considerations

Новых критичных пропусков не обнаружено.

## 6. Итог

Оба блокирующих замечания закрыты с rationale и расширенной верификацией; все четыре non-blocking рекомендации применены. План готов к hand-off в developer-агентов по wave-plan (Wave 1 = {1,2,5,7}, далее последовательно 3→4→6→8→9).

**VERDICT**: `APPROVED`
**BLOCKING_ISSUES**: 0
