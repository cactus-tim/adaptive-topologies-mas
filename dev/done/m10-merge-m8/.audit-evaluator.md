# Audit — _evaluator.py absence (Step 3.4)

Date: 2026-05-13
Branch: feat/m10 @ d2f57bc (after Wave 2 HITL bring-in)

## File existence
`src/atm/experiment/_evaluator.py` — **ABSENT** (correctly).

## Grep results
- `grep -rn "from atm.experiment._evaluator" src/` → **0 matches**
- `grep -rn "from atm.experiment._evaluator" tests/` → **0 matches**
- `grep -rn "atm.experiment._evaluator" src/ tests/` → **0 matches**

## Conclusion
The M6 stub has been correctly removed in M10/M11 and Wave 2's import of HITL tests did NOT re-introduce any reference to `_evaluator`. No follow-up action needed for Wave 7.2 regarding `_evaluator` imports.

## Note for Wave 7.2
The m9.2-brought tests (`tests/unit/experiment/test_runner_role_router.py`, `test_runner_human_role.py`, `test_runner_initial_state.py`) may still mock `evaluate(cfg.task, final_answer)` directly on `atm.experiment.runner.evaluate` — this is a DIFFERENT pattern from `_evaluator` import. Wave 7.2 audit should grep separately:
- `grep -rn "runner.evaluate\|atm.experiment.runner.evaluate" tests/`
- `grep -rn "patch.*evaluate" tests/`
