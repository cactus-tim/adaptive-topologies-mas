# Integration test metadata — m12-grid-integration

The 4 PG-gated acceptance tests for this block ARE the M12 exit-criteria suite:

| Exit criterion | Test file |
|---|---|
| EC-1: `atm grid` runs N cells in parallel | `tests/integration/experiment/test_m12_exit_grid_parallel.py::test_grid_parallel_all_cells_complete` |
| EC-2: `atm resume` recovers SIGKILLed run | `tests/integration/experiment/test_m12_exit_resume_kill.py::test_resume_after_sigkill_m12_exit_criterion` |
| EC-3: `atm replay` produces bitwise-identical state | `tests/integration/experiment/test_m12_exit_replay_bitwise.py::test_replay_bitwise_via_subprocess` |
| EC-4: `atm grid` reconciles zombie runs on start | `tests/integration/experiment/test_m12_exit_reconcile.py::test_reconcile_zombie_on_grid_start` |

**Run command:** `ATM_ENABLE_PG_TESTS=1 uv run pytest tests/integration/experiment/test_m12_exit_*.py -v`

All four tests use `subprocess.run(["uv", "run", "atm", ...])` instead of CliRunner to avoid asyncio_mode=auto event-loop conflict.

No additional Phase 4 acceptance tests required — these ARE the acceptance suite.
