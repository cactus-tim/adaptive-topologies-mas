# M8 Audit Baseline — Step 1.1

> Read-only аудит 5 существующих M1/M3-артефактов перед началом имплементации M8.
> Дата: 2026-05-11. Тесты: `uv run pytest tests/unit/core -q` → **168 passed**.

---

## Аудируемые артефакты

| # | Файл | Milestone |
|---|------|-----------|
| 1 | `src/atm/core/types.py` | M1 |
| 2 | `src/atm/core/state.py` | M1 |
| 3 | `src/atm/core/reducers.py` | M1 |
| 4 | `src/atm/storage/models.py` | M3 |
| 5 | `alembic/versions/0001_initial_business_schema.py` | M3 |

Дополнительно проверен: `src/atm/storage/schemas.py` (Parquet-схемы, M3).

---

## Чеклист сверки (13 колонок × 5 артефактов)

### 1. `src/atm/core/types.py`

| Колонка | Статус | Детали |
|---------|--------|--------|
| Файл существует | OK | |
| Pydantic v2 (`BaseModel`, `ConfigDict(frozen=True)`) | OK | Все domain-модели frozen |
| `AgentRole` enum | OK | 6 значений: PLANNER/RESEARCHER/EXECUTOR/CRITIC/DEBATER/COORDINATOR |
| `AgentRole` — наличие 7-го Adaptive-роста | **GAP** | Arch.md §17 (deviation #11) требует 7 ролей для Adaptive: Debater_pro/Debater_contra как отдельные значения или механизм параметризации. Текущий enum имеет только `DEBATER`. Нужно решить в M8.5 (при обновлении Debater). |
| `Phase` enum (4 значения) | OK | PLANNING/EXECUTION/VERIFICATION/DONE |
| `HumanRole` enum (5 значений) | OK | COORDINATOR/REVIEWER/JUDGE/PEER/MONITOR |
| `MessageKind` enum (6 значений) | OK | REQUEST/BROADCAST/DRAFT/CRITIQUE/DECISION/PHASE_EMIT |
| `Message` (frozen, id/sender/recipients/kind/content/payload/refs/created_at) | OK | `to_lc` и `from_lc` реализованы |
| `TopologyTransition` (frozen, все поля M8-схемы) | OK | id/run_id/from_topology/to_topology/phase_at_decision/iter_within_phase/iter_within_topology/decided_by/reason/considered_alternatives/guards_applied/signals_snapshot/router_cost_usd/at |
| `PhaseTransition` (frozen, поля из arch.md §8.3) | OK | id/run_id/from_phase/to_phase/entry_reason/iter_total/decided_by/at |
| `TopologyDecision` (frozen Pydantic) | **MISSING** | Тип `TopologyDecision` (arch.md §8bis.1) отсутствует в types.py. Нужен для M8.3 (TopologyRouter). Добавить в M8.1. |
| `PhaseDecision` (frozen Pydantic) | **MISSING** | Тип `PhaseDecision` (arch.md §8.2) отсутствует в types.py. Нужен для M8.2 (PhaseManager). Добавить в M8.1. |
| `LLMResponse.started_at` (для dedup sort_by) | OK | Поле присутствует |
| `_utcnow()` использует `datetime.now(UTC)` (не deprecated `utcnow`) | OK | Реализация корректная |

### 2. `src/atm/core/state.py`

| Колонка | Статус | Детали |
|---------|--------|--------|
| Файл существует | OK | |
| TypedDict (не BaseModel) — соответствует arch.md §3.2 | OK | `AgentState`, `SharedState`, `GraphState` — все TypedDict |
| `AgentState` поля (agent_id/role/inbox/outbox/scratchpad/tool_calls/tool_results/summary_before_window/step_count/tokens_spent/cost_spent_usd) | OK | Все 11 полей присутствуют |
| `SharedState` — Phase-ось (phase/phase_started_at_iter/phase_history) | OK | |
| `SharedState` — Topology-ось для L2 (active_topology/topology_started_at_iter/topology_switch_count/topology_history) | OK | Все 4 поля |
| `SharedState` — iter-счётчики (iter_total/iteration) | OK | |
| `SharedState` — signals (для L2 quasi-preemption) | OK | |
| `SharedState` — HITL (human_requests/human_responses) | OK | |
| `SharedState` — broadcast_bus | OK | |
| `GraphState` — `agents` Annotated с `merge_agent_states` | OK | |
| `GraphState` — `messages` Annotated с `dedup_by_id_reducer("id", sort_by="created_at")` | OK | |
| `GraphState` — `llm_calls` Annotated с `dedup_by_id_reducer("id", sort_by="started_at")` | OK | |
| `GraphState` — `budget_events` Annotated с `dedup_by_id_reducer("id", sort_by="at")` | OK | |
| `GraphState` — `topology_transitions` Annotated с `dedup_by_id_reducer("id", sort_by="at")` | OK | |

### 3. `src/atm/core/reducers.py`

| Колонка | Статус | Детали |
|---------|--------|--------|
| Файл существует | OK | |
| `merge_agent_states(left, right) -> dict` | OK | Подпись и возврат корректны |
| Инвариант: None/empty нейтральный элемент | OK | `if not left: return dict(right or {})` |
| Инвариант: не мутирует входные аргументы | OK | Использует `list()` копии |
| Инвариант: inbox/outbox/scratchpad/tool_calls/tool_results — concat (left+right) | OK | |
| Инвариант: step_count/tokens_spent/cost_spent_usd — max | OK | |
| Инвариант: summary_before_window — right wins if non-empty | OK | |
| `dedup_by_id_reducer(key, *, sort_by=None)` — фабрика | OK | Возвращает closure `_reduce` |
| Closure именована `_reduce` (требование MC-3) | OK | `def _reduce(...)` внутри фабрики |
| Инвариант: idempotency, left-wins на коллизии | OK | `seen` set строится из `left` |
| Инвариант: сортировка по `sort_by` ASC если указан | OK | |
| Поддержка dict и object (getattr) в `_get` | OK | `isinstance(item, dict)` ветвление |

### 4. `src/atm/storage/models.py`

| Колонка | Статус | Детали |
|---------|--------|--------|
| Файл существует | OK | |
| 6 бизнес-таблиц | OK | Experiment/Run/Phase/HumanInteraction/BudgetEvent/TopologyTransition |
| `topology_transitions` таблица (SQLAlchemy model) | OK | Все поля arch.md §3.4 присутствуют |
| `topology_transitions` — ARRAY(String) для `considered_alternatives`/`guards_applied` | OK | `server_default="'{}'::text[]"` |
| `topology_transitions` — JSONB для `signals_snapshot` | OK | `server_default="'{}'::jsonb"` |
| `topology_transitions` — 3 индекса (run_id, decided_by, at) | OK | Совпадают с DDL arch.md |
| `runs` — reproducibility bundle (models_by_role_json/model_version_snapshot/sandbox_image_digest) | OK | |
| `runs` — finish_reason (VARCHAR 32) | OK | |
| `runs` — 5 индексов | OK | exp_id/topology/task_id/status/started_at |
| `HumanInteraction` — request_id (idempotency key) | OK | VARCHAR(64), nullable |
| `HumanInteraction` — raw_tlx_score (aggregated float) | OK | |
| Все relationships — `lazy="raise"` | OK | |
| `Base(DeclarativeBase)` — единая base | OK | |

### 5. `alembic/versions/0001_initial_business_schema.py`

| Колонка | Статус | Детали |
|---------|--------|--------|
| Файл существует | OK | revision="0001" |
| FK-safe порядок создания (experiments → runs → phases/human_interactions/budget_events/topology_transitions) | OK | |
| `topology_transitions` DDL — ARRAY/JSONB с server_default | OK | `'{}'::text[]` / `'{}'::jsonb` |
| `topology_transitions` DDL — 3 индекса | OK | |
| `runs` DDL — reproducibility bundle колонки | OK | |
| `down_revision = "bc5f66dd0897"` (initial Alembic migration) | OK | |
| `downgrade()` — правильный reverse порядок | OK | |

---

## Parquet-схемы (`src/atm/storage/schemas.py`)

| Аспект | Статус | Детали |
|--------|--------|--------|
| `TOPOLOGY_TRANSITION_SCHEMA` существует | OK | |
| Поля соответствуют arch.md §3.5 (canonical names) | OK | Закрыто reconciliation note в файле (step 4.1) |
| `considered_alternatives` и `guards_applied` сериализованы как JSON string | OK | `considered_alternatives_json`, `guards_applied_json` — отличие от arch.md §3.5 (там `list_(pa.string())`); решение принято в step 4.1 ради schema-стабильности |
| Timestamp колонки — `pa.timestamp('us', tz='UTC')` | OK | Корректнее чем naive timestamp из arch.md §3.5 |

---

## Сводка: что нужно добавить в M8.1

| # | Что | Где | Когда |
|---|-----|-----|-------|
| 1 | `TopologyDecision` (frozen Pydantic, поля: topology/reason/decided_by/considered_alternatives/router_cost_usd) | `core/types.py` | M8.1 (этот шаг) |
| 2 | `PhaseDecision` (frozen Pydantic, поля: next_phase/reason/decided_by) | `core/types.py` | M8.1 (этот шаг) |
| 3 | Решение по `AgentRole` для 7-го Adaptive-роста (Debater_pro/Debater_contra или stance-параметр) | `core/types.py` | M8.5 (при обновлении Debater) |
| 4 | Alembic migration для `topology_transitions` | уже в 0001 — **ничего не нужно** | — |

## Что уже готово и не требует изменений в M8.1

- `core/reducers.py` — полностью готов (dedup_by_id_reducer + merge_agent_states)
- `core/state.py` — полностью готов (SharedState содержит все L2-поля)
- `core/types.py` — `TopologyTransition` и `PhaseTransition` уже есть
- `storage/models.py` — `TopologyTransition` ORM-модель с правильным DDL
- `alembic/0001` — topology_transitions таблица уже создаётся

## Тестовое покрытие (baseline)

- `tests/unit/core/` — **168 тестов**, все проходят
- Покрыты: reducers (21+14 тестов), state (14 тестов), types, errors, LC-adapter, public API
- Не покрыты пока: `TopologyDecision`, `PhaseDecision` (будут добавлены в M8.1)

```
168 passed in 1.01s
```
