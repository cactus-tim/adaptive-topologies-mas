# M9.1 — HITL integration в остальные 5 топологий — Tasks

---

## Wave 1 — Foundation

### Phase 1: HumanCfg.extra + node-factory helper + Runner human_role COMPLETE

- [x] 1.1 Добавить `extra: dict[str, Any] | None = None` в `HumanCfg` — `src/atm/experiment/config.py`
  - Type: tdd
  - Depends On: nothing
  - Can-Parallel-With: nothing (Wave 1 foundation)
  - Acceptance: `HumanCfg(enabled=True, role=..., gateway=..., extra={"judge": "human"})` проходит валидацию; existing config без `extra` не ломается; frozen Pydantic совместимо.

- [x] 1.2 CREATE `src/atm/human/_node_factory.py` — `build_human_node_factory(...)` с module-level lazy imports
  - Type: tdd
  - Depends On: 1.1
  - Can-Parallel-With: nothing
  - Acceptance: module-level try/except для `LLMSimulatedGateway`, `CLIGateway`, `request_with_timeout`; сигнатура принимает `topology_name`, `human_cfg`, `gateway`, `request_id_template`, `question_extractor`, `apply_decision`; async callable, совместимый с LangGraph node signature; `apply_decision` default: approve→`shared["human_approved"]=True`, reject→Message + `needs_rerun=True`.

- [x] 1.3 Re-export `build_human_node_factory` из `src/atm/human/__init__.py`
  - Type: simple
  - Depends On: 1.2
  - Can-Parallel-With: nothing
  - Acceptance: `from atm.human import build_human_node_factory` работает.

- [x] 1.4 Добавить `human_role` в `Runner._insert_run` — `src/atm/experiment/runner.py`
  - Type: tdd
  - Depends On: 1.1
  - Can-Parallel-With: 1.2
  - Acceptance: `human_role=cfg.human.role.value if cfg.human and cfg.human.enabled else None` передаётся в `Run(...)`; поле уже в модели `storage/models.py:138`.

- [x] 1.5 Unit тесты: factory (6+ тестов), runner human_role (3 теста), config extra
  - Type: tdd
  - Depends On: 1.2, 1.4
  - Can-Parallel-With: nothing
  - Files: `tests/unit/human/test_node_factory.py` (CREATE), `tests/unit/experiment/test_runner_human_role.py` (CREATE), `tests/unit/experiment/test_config_human.py` (modify)
  - Acceptance: все зелёные; `uv run pytest tests/unit/human/test_node_factory.py tests/unit/experiment/test_runner_human_role.py tests/unit/experiment/test_config_human.py -q`

- [x] 1.6 mypy + ruff Wave 1
  - Type: simple
  - Depends On: 1.5
  - Can-Parallel-With: nothing
  - Acceptance: `uv run mypy src/atm/human/_node_factory.py src/atm/experiment/runner.py src/atm/experiment/config.py` — чисто; `uv run ruff check src/atm/human/ src/atm/experiment/` — чисто.

---

## Wave 2 — Topology Integration (параллельно после Wave 1)

### Phase 2a: Star COMPLETE

- [x] 2.1 Star HITL — `human_reviewer` + `_route_from_coord` override — `src/atm/topology/star.py`
  - Type: tdd
  - Depends On: 1.3 (Wave 1 complete)
  - Can-Parallel-With: 2.4, 2.7, 2.10, 2.13
  - Acceptance: граф с `enabled=True, role=Reviewer` вставляет `human_reviewer` после `critic_postprocess`; `_route_from_coord` читает `signals["human_phase_override"]` как первую проверку и очищает сигнал после прочтения (`signals["human_phase_override"]=None`); override values: `"advance"`, `"stay"`, `"finalize"`; без `human_cfg.enabled` граф идентичен M7.

- [x] 2.2 Star unit тест (7+ тестов) — `tests/unit/topology/test_star_human.py`
  - Type: tdd
  - Depends On: 2.1
  - Can-Parallel-With: 2.5, 2.8, 2.11, 2.15
  - Acceptance: graph structure; dispatch order; approve/reject Reviewer; Coordinator-override force-advance/force-stay/no-override; back-compat (без signal route unchanged).

- [x] 2.3 Star integration тест — `tests/integration/human/test_hitl_star.py` + `tests/fixtures/llm/m91_star_*.yaml`
  - Type: tdd
  - Depends On: 2.2
  - Can-Parallel-With: 2.6, 2.9, 2.12, 2.16
  - Acceptance: `ATM_ENABLE_PG_TESTS=1 uv run pytest tests/integration/human/test_hitl_star.py -q` зелёный; `human_interactions count ≥ 1`; `runs.human_role='reviewer'`.

- [x] 2.3b Star back-compat проверка
  - Type: simple
  - Depends On: 2.3
  - Can-Parallel-With: 2.6b, 2.9b, 2.12b, 2.16b
  - Acceptance: `uv run pytest tests/unit/topology/test_star.py -q` — зелёный (M7 back-compat); `uv run mypy src/atm/topology/star.py` — чисто.

### Phase 2b: Mesh COMPLETE

- [x] 2.4 Mesh HITL — `human_peer` + `consensus_pending` — `src/atm/topology/mesh.py`
  - Type: tdd
  - Depends On: 1.3 (Wave 1 complete)
  - Can-Parallel-With: 2.1, 2.7, 2.10, 2.13
  - Acceptance: `human_peer` добавляется в `agent_order` при `enabled=True`; до `activation_round` (default 2) — dispatcher пропускает human_peer; `mesh_postprocess` выставляет `signals["consensus_pending"]` при split-vote (не ломает consensus_reached); без `human_cfg.enabled` граф идентичен M7.

- [x] 2.5 Mesh unit тест (6+ тестов) — `tests/unit/topology/test_mesh_human.py`
  - Type: tdd
  - Depends On: 2.4
  - Can-Parallel-With: 2.2, 2.8, 2.11, 2.15
  - Acceptance: graph structure; activation_round behavior; consensus_pending trigger; round-robin honor; on_consensus_pending mode.

- [x] 2.6 Mesh integration тест — `tests/integration/human/test_hitl_mesh.py` + `tests/fixtures/llm/m91_mesh_*.yaml`
  - Type: tdd
  - Depends On: 2.5
  - Can-Parallel-With: 2.3, 2.9, 2.12, 2.16
  - Acceptance: deterministic 2-round scenario — `m91_mesh_consensus.yaml`: threshold=4, 3 LLM peers vote "X" uniformly (round 1: max_votes=3 < 4 → pending), round 2: human votes "X" → consensus; `human_interactions count ≥ 1`; `runs.human_role='reviewer'`.

- [x] 2.6b Mesh back-compat проверка
  - Type: simple
  - Depends On: 2.6
  - Can-Parallel-With: 2.3b, 2.9b, 2.12b, 2.16b
  - Acceptance: `uv run pytest tests/unit/topology/test_mesh.py tests/integration/topology/test_mesh_e2e.py -q` зелёный; `uv run mypy src/atm/topology/mesh.py` чисто.

### Phase 2c: Debate COMPLETE

- [x] 2.7 Debate HITL — `human_judge` (mode="human") + `_both_judge_postprocess` (mode="both") — `src/atm/topology/debate.py`
  - Type: tdd
  - Depends On: 1.3 (Wave 1 complete)
  - Can-Parallel-With: 2.1, 2.4, 2.10, 2.13
  - Acceptance: mode="critic" — no-op; mode="human" — узел `judge` заменён на `human_judge` (synthesized DECISION в `agents[judge_id]["outbox"]`), module-level `_judge_postprocess` НЕ трогается; mode="both" — `_both_judge_postprocess` inline closure: (1) LLM-judge inline, (2) human gateway, (3) aggregate human > critic — sequential, module-level `_judge_postprocess` не вызывается; без `human_cfg.enabled` граф идентичен M7.

- [x] 2.8 Debate unit тест (7+ тестов) — `tests/unit/topology/test_debate_human.py`
  - Type: tdd
  - Depends On: 2.7
  - Can-Parallel-With: 2.2, 2.5, 2.11, 2.15
  - Acceptance: 3 режима × pro/contra wins; execution order для "both" (LLM-then-human); synthesized DECISION message format соответствует тому что `_judge_postprocess` ожидает.

- [x] 2.9 Debate integration тест — `tests/integration/human/test_hitl_debate.py` + `tests/fixtures/llm/m91_debate_*.yaml`
  - Type: tdd
  - Depends On: 2.8
  - Can-Parallel-With: 2.3, 2.6, 2.12, 2.16
  - Acceptance: mode="human" → run завершается; `human_interactions count = 1`; `runs.human_role` корректен.

- [x] 2.9b Debate back-compat проверка
  - Type: simple
  - Depends On: 2.9
  - Can-Parallel-With: 2.3b, 2.6b, 2.12b, 2.16b
  - Acceptance: `uv run pytest tests/unit/topology/test_debate.py tests/integration/topology/test_debate_e2e.py -q` зелёный; `uv run mypy src/atm/topology/debate.py` чисто.

### Phase 2d: Hierarchical COMPLETE

- [x] 2.10 Hierarchical HITL — `human_top_reviewer` (scope=top) + `human_sub_reviewer` (scope=sub_team) + WARNING docstring — `src/atm/topology/hierarchical.py`
  - Type: tdd
  - Depends On: 1.3 (Wave 1 complete)
  - Can-Parallel-With: 2.1, 2.4, 2.7, 2.13
  - Acceptance: scope="top" — `human_top_reviewer` между `top_coord` и conditional; scope="sub_team" — `human_sub_reviewer` в каждом subgraph, без `interrupt()` API; WARNING docstring на subgraph helper (тема: CLIGateway limitation, deferred M9.2); request_id для sub_team: `f"hierarchical:{team_id}:{iter_total}:reviewer"`; без `human_cfg.enabled` граф идентичен M7.

- [x] 2.11 Hierarchical unit тест (6+ тестов) — `tests/unit/topology/test_hierarchical_human.py`
  - Type: tdd
  - Depends On: 2.10
  - Can-Parallel-With: 2.2, 2.5, 2.8, 2.15
  - Acceptance: scope=top vs sub_team graph structure; dispatch order для top; WARNING docstring присутствует.

- [x] 2.12 Hierarchical integration тест (2 сценария) — `tests/integration/human/test_hitl_hierarchical.py` + `tests/fixtures/llm/m91_hierarchical_*.yaml`
  - Type: tdd
  - Depends On: 2.11
  - Can-Parallel-With: 2.3, 2.6, 2.9, 2.16
  - Acceptance: scope=top сценарий — 1 HITL вызов; scope=sub_team сценарий — explicit assertion `len(rows) == 2`, team_a + team_b в request_ids (доказывает callback propagation через subgraph); `runs.human_role` корректен для обоих.

- [x] 2.12b Hierarchical back-compat проверка
  - Type: simple
  - Depends On: 2.12
  - Can-Parallel-With: 2.3b, 2.6b, 2.9b, 2.16b
  - Acceptance: `uv run pytest tests/unit/topology/test_hierarchical.py tests/integration/topology/test_hierarchical_e2e.py -q` зелёный; `uv run mypy src/atm/topology/hierarchical.py` чисто.

### Phase 2e: Adaptive COMPLETE

- [x] 2.13 Widen `decided_by` Literal в `TopologyDecision` и `TopologyTransition` — `src/atm/core/types.py`
  - Type: simple
  - Depends On: 1.3 (Wave 1 complete)
  - Can-Parallel-With: 2.1, 2.4, 2.7, 2.10
  - Acceptance: `TopologyDecision(decided_by="human_override", ...)` и `TopologyTransition(decided_by="human_override", ...)` создаются без ValidationError; `uv run mypy src/atm/core/types.py` чисто; no alembic migration; unit test явно инстанциирует оба типа с `"human_override"`.

- [x] 2.14 Adaptive HITL — `human_advisor` (advisory + override) — `src/atm/topology/adaptive.py`
  - Type: tdd
  - Depends On: 2.13
  - Can-Parallel-With: 2.2, 2.5, 2.8, 2.11 (после 2.13)
  - Acceptance: advisory mode (default) — `signals["human_advisor_hint"]` = response.action, routing не меняется; override mode (`human_can_override_router=True`, `action="switch_topology"`) — `TopologyDecision` перезаписан, `TopologyTransition.decided_by="human_override"`, SwitchGuards уважаются; invalid override target → silent ignore + log.warning + hint; без `human_cfg.enabled` граф идентичен M8.6; docstring: subgraph interrupt-resume deferred to M9.2.

- [x] 2.15 Adaptive unit тест (7+ тестов) — `tests/unit/topology/test_adaptive_human.py`
  - Type: tdd
  - Depends On: 2.14
  - Can-Parallel-With: 2.2, 2.5, 2.8, 2.11
  - Acceptance: advisory mode; override mode; guard-respect; invalid target → ignored; `decided_by="human_override"` emitted; unit test инстанциирует `TopologyDecision/Transition(decided_by="human_override")` — passes only after Literal widening.

- [x] 2.16 Adaptive integration тест — `tests/integration/human/test_hitl_adaptive.py` + `tests/fixtures/llm/m91_adaptive_*.yaml`
  - Type: tdd
  - Depends On: 2.15
  - Can-Parallel-With: 2.3, 2.6, 2.9, 2.12
  - Acceptance: advisory scenario — `topology_transitions.decided_by` не содержит `"human_override"`; override scenario — `SELECT decided_by FROM topology_transitions WHERE run_id=... AND decided_by='human_override'` count ≥ 1.

- [x] 2.16b Adaptive back-compat проверка
  - Type: simple
  - Depends On: 2.16
  - Can-Parallel-With: 2.3b, 2.6b, 2.9b, 2.12b
  - Acceptance: `uv run pytest tests/integration/test_adaptive_m8.py -q` зелёный; `uv run mypy src/atm/topology/adaptive.py src/atm/core/types.py` чисто.

---

## Wave 3 — Cross-topology acceptance + docs

### Phase 3: Cross-topology acceptance + codebase-map COMPLETE

- [x] 3.1 Cross-topology parametrized integration тест — `tests/integration/human/test_hitl_cross_topology.py`
  - Type: tdd
  - Depends On: 2.3, 2.6, 2.9, 2.12, 2.16 (все Wave 2 complete)
  - Can-Parallel-With: 3.2
  - Acceptance: 6 параметров:
    - `("chain", None)` — existing M9 config
    - `("star", None)` — default reviewer
    - `("mesh", {"activation_round": 2})` — deterministic 2-round
    - `("debate", {"judge": "human"})` — обязательно, иначе no-op
    - `("hierarchical", {"scope": "top"})` — 1 row
    - `("adaptive", None)` — advisory mode
    - Каждый: `human_interactions count ≥ 1`; `runs.human_role = 'reviewer'`; `ATM_ENABLE_PG_TESTS=1 uv run pytest tests/integration/human/test_hitl_cross_topology.py -q` зелёный.

- [x] 3.2 Обновить `dev/codebase-map.md` — секция M9.1
  - Type: simple
  - Depends On: Wave 2 all complete (conceptual — можно писать до)
  - Can-Parallel-With: 3.1
  - Acceptance: секция содержит: все 6 топологий HITL, per-topology design choices (Debate judge-mode, Hierarchical scope, Adaptive advisory/override), deferred M9.2 (subgraph interrupt-resume); `git diff dev/codebase-map.md` показывает добавленное.

- [ ] 3.3 Полный регресс
  - Type: simple
  - Depends On: 3.1, 3.2
  - Can-Parallel-With: nothing
  - Acceptance: `uv run pytest tests/ -q` — все тесты зелёные (без `ATM_ENABLE_PG_TESTS`); `ATM_ENABLE_PG_TESTS=1 uv run pytest tests/integration/human/ -q` — все integration зелёные.

---

## Stats

- Total: 26 tasks (~14h estimated)
- Done: 25 / 26

## How to Update

После завершения каждой задачи:
1. Поставить `[x]` вместо `[ ]`.
2. Обновить заголовок Phase: если все задачи в Phase Done → `COMPLETE`, если часть → `IN PROGRESS`.
3. Обновить `SESSION PROGRESS` в `m9.1-context.md`.
4. Обновить Stats: `Done: N / 26`.
